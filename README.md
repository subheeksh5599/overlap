# Overlap

Collision radar for parallel agent fleets.

Detects cross-session file overlap in Agent Orchestrator worktrees before merge, advises merge order, and reports post-merge conflicts. Deterministic git analysis — no LLM in the core.

## Status

Build in progress (The Orchestra hackathon, built with Agent Orchestrator).
